
[default]

# Encoding fails due to a cyclic depencency with 'total sectors:' /
# 'num clusters' and the length reference.
vfat/fat12=encode

[Python]

# Missing instance 'exif' from 'define-huffman-table'
jpeg/smiley=encode
# Missing frame elments...
mp3/tone=encode
# Failure decoding encoded data (Expected ${digit} <= 57; got 111)
pdf/hello=encode



[C]

# Solving division... both numerator & denominator a constants though!
mp3/tone=encode
# Compilation errors in encoding code
pdf/hello=encode
