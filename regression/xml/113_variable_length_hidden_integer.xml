<protocol>
    <sequence name="a">
        <sequence name="b:" value="${value:}">
            <field name="len:" length="8" />
            <field name="value:" length="${len:} * 8" />
        </sequence>
        <sequence name="c" value="${b:}" />
    </sequence>
</protocol>
