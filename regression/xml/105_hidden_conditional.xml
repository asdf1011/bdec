<protocol>
    <sequence name="a">
        <field name="has footer:" length="8" />
        <sequence name="footer" if="${has footer:}">
            <field name="b" length="8" type="integer" />
        </sequence>
    </sequence>
</protocol>
