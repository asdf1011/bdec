<protocol>
    <!--
      Test that when we hide a parent entry, all child entries are also hidden.
      -->
    <sequence name="a">
        <sequence name="">
            <field name="b" length="8" type="integer" />
        </sequence>
    </sequence>
</protocol>
